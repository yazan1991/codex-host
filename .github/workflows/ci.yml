name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

# Superseded PR runs are disposable; main runs provide per-commit release evidence.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  check:
    name: Check ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-22.04
          - macos-14
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm

      - name: Setup npm
        run: npm install --global npm@11.8.0

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.97.1

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        if: runner.os == 'Linux'
        run: npm run format:check

      - name: Lint
        if: runner.os == 'Linux'
        run: npm run lint

      - name: Typecheck
        if: runner.os == 'Linux'
        run: npm run typecheck

      - name: Test TypeScript (full suite)
        if: runner.os == 'Linux'
        run: npm run test:typescript

      # Repository governance runs on Linux in production, not on Desktop platforms.
      - name: Test TypeScript (cross-platform)
        if: runner.os != 'Linux'
        run: npm run test:typescript -- --exclude 'packages/repository-automation/test/**'

      - name: Check Rust
        run: npm run check:rust

      - name: Smoke-test Linux npm package
        if: runner.os == 'Linux'
        run: >
          node scripts/release/smoke-npm-package.mjs
          --target linux-x64
          --version 0.1.6-ci.0

  check-linux-arm64:
    name: Check Linux ARM64
    runs-on: ubuntu-22.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm

      - name: Setup npm
        run: npm install --global npm@11.8.0

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.97.1

      - name: Install dependencies
        run: npm ci

      - name: Test TypeScript (cross-platform)
        run: npm run test:typescript -- --exclude 'packages/repository-automation/test/**'

      - name: Check Rust
        run: npm run check:rust

      - name: Smoke-test Linux ARM64 npm package
        run: >
          node scripts/release/smoke-npm-package.mjs
          --target linux-arm64
          --version 0.1.6-ci.0
